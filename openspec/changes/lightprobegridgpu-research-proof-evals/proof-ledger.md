# Proof Ledger: LightProbeGridGPU Research-Proof Evals

| Item | Status | Evidence | Decision |
|------|--------|----------|----------|
| Runtime GPU-resident boundary | SUPPORTED | Source invariants guard no `readRenderTargetPixels`, `readPixels`, or `Data3DTexture` in runtime. | Preserve. |
| Runtime runner predicates | SUPPORTED | Runtime smoke assertions now validate bake coalescing, benchmark backend/memory/visibility-depth/timing, and color-sanity raw facts through named local predicates. | Keep runtime runner checks predicate-owned; avoid scattered timing/memory payload-shape chains. |
| Profiler payload compaction | SUPPORTED | Current diff keeps medians, measured run counts, backend pass/fallback, timing sources, static work. | Keep if checks pass. |
| Escape classification compaction | SUPPORTED | Current diff derives aggregate escape counts once from escaped probes. | Keep if visibility assertions pass. |
| Receiver normal verdict ownership | SUPPORTED | Harness now emits raw receiver/shader agreement facts; `receiverNormal.frontFaceShaderAgreement` is evaluated in proof gates. | Keep. |
| Visibility weighting verdict ownership | SUPPORTED | Study now emits raw directional suppression and escape facts; `visibilityWeighting.directionalSuppression` is evaluated in proof gates. | Keep. |
| Visibility runner predicates | SUPPORTED | Visibility smoke assertions now validate moment readback, visibility weighting, escape classification, SH contribution, receiver-surface, and receiver-normal raw facts through named local predicates guarded by source invariants. | Keep visibility runner checks predicate-owned; avoid scattered inline payload-shape chains. |
| Receiver surface verdict ownership | SUPPORTED | Receiver diagnostics emit raw surface render/CPU wrong-ratio facts; `receiverSurface.cpuRenderAgreement` and `surfaceCpuRenderDelta` are evaluated in proof gates. | Keep masked render ratios out of the receiver-surface agreement path. |
| Receiver surface agreement ownership | SUPPORTED | Receiver diagnostics now emit raw `renderSurfaceWrongRatio` plus `surfaceRuntimeWrongRatioMax`; proof gates derive `surfaceCpuRenderDelta` once with matching max-to-max aggregation and feed that value to both predicate and gate actual. | Keep receiver-surface agreement deltas out of diagnostics; do not compare render max against whichever CPU aggregate happens to be closer. |
| Receiver surface runtime mirror | SUPPORTED | SH contribution diagnostics now mirror runtime visibility-moment composition by deriving `visibilityMass` from visibility weight over scalar weight and applying it after visibility irradiance evaluation, instead of blending coefficients with the stale `visibilityWeightFloor` denominator. | Rerun focused eval proof to see whether the receiver-surface CPU/render gate closes; keep the gate open if rendered evidence still disagrees. |
| SH contribution verdict ownership | SUPPORTED | SH diagnostics now emit raw mixed-color row count and ratio facts; `shContribution.noBakedMixedColorRisk` is evaluated in proof gates. | Keep. |
| SH mixed-color risk predicate | SUPPORTED | The SH risk gate now treats intermediate `visibilityWrongRatioMean` as raw diagnostic evidence only and blocks on final `runtimeWrongRatioMean`. | Keep intermediate visibility aggregate drift and visibility escape evidence out of SH promotion math unless a later gate explicitly proves they map to final runtime leakage. |
| Visibility escape gate ownership | SUPPORTED | Refreshed proof showed low SH runtime wrong-ratio (`0.0318`) with `wrongSideEscapedCount = 4`; wrong-side escape is now owned by `visibilityWeighting.noWrongSideEscape` instead of the SH mixed-color gate, producing 13 supported / 3 open gates. Follow-up compact evidence showed `visibilityBypassEscapeCount = 0` and `frontEdgeBypassEscapeCount = 0`, so the remaining escapes are true sealed-wall moment visibility escapes. | Keep escape classification in visibility gates; do not use SH gates as a catch-all for visibility escape evidence. |
| True escape reason evidence | SUPPORTED | Refreshed Cornell proof smoke still reports `wrongSideEscapedCount = 4`, with visibility-bias and front-edge bypass counts both `0`. Initial reason evidence split the escapes as receiver-before-mean `2` and weak-crush `2`; after double-sided visibility-distance capture, receiver-before-mean dropped to `0` and weak-crush rose to `4`. | Treat the remaining escape gate as a moment-heuristic/visibility-weighting limitation until a source-backed runtime fix is identified; do not hide it behind SH risk or baseline comparisons. |
| Double-sided visibility distance capture | SUPPORTED | Visibility-distance override material now uses `DoubleSide`, and refreshed Cornell proof smoke still passes 33 checks with 13 supported / 3 open gates. The change removed receiver-before-mean escape evidence but did not close the weak-crush escape gate or residual leak gate. | Keep double-sided capture because visibility moments should not depend on mesh front-face orientation; do not claim it fixes the sealed-wall residual leak. |
| Weak-crush no-hit classification | SUPPORTED | Compact gate evidence now splits weak-crush escapes into partial-hit, full-hit, no-hit, and near-threshold counts. Refreshed Cornell proof smoke reports all four remaining wrong-side escapes as `weakCrushNoHitEscapeCount = 4`, with partial/full/near-threshold counts at `0`. | Treat the remaining visibility escape as moment coverage miss evidence; do not keep tuning hit-confidence or Chebyshev blending without a source-backed coverage change. |
| No-hit escape localization | SUPPORTED | The open `visibilityWeighting.noWrongSideEscape` gate now carries `visibilityDepthResolution = 8`, `weakCrushNoHitCrossingCount = 4`, visibility/surface divider segment hit counts `4/4`, receiver/probe direction mismatch `0`, adjacent oct-bin hits `4`, all-adjacent-no-hit misses `0`, and distinct oct bins `4`. | Classify the four residual escapes as an 8px octahedral visibility-moment exact-bin coverage limitation; keep the gate open honestly because `wrongSideEscapedCount` remains `4`. |
| Adjacent visibility-moment rescue SDD | SUPPORTED | Design now defines the goal, ideal state, non-goals, conditional cross-bin runtime candidate, cost-ceiling facts, over-occlusion safeguards, acceptance outcome, and rejection outcome for addressing `visibilityWeighting.noWrongSideEscape`. | Start with conditional cross-bin fallback only when exact-bin `hitConfidence` is zero; reject any candidate that closes escapes by over-suppressing correct-side contribution or weakening residual-leak honesty. |
| Adjacent cross-bin fallback prototype | REJECTED | Conditional cross-bin fallback generated compact fallback facts: activation `11`, adjacent loads `44`, rescue count `5`, no-adjacent-hit count `6`, while `wrongSideEscapedCount` remained `4`; residual leak remained independently open at `0.942`. The unconditional-cross upper bound still left `wrongSideEscapedCount = 4` and opened directional suppression (`0.1838`) plus receiver-surface agreement (`0.2422`). | Do not promote adjacent cross-bin lookup; keep it out of runtime/mirror code and keep the escape classified as an 8px exact-bin visibility-moment limitation. |
| Cross-bin bake dilation | SUPPORTED | GPU visibility-moment repack now includes one-pixel cardinal taps in addition to center and half-pixel cardinal taps. Focused Cornell proof remains 14 supported / 2 open; `wrongSideEscapedCount` stays `4`, but pre-tone masked leak improves from `0.8126` to `0.8089` and residual leak ratio improves from `0.942` to `0.9377` without adding runtime sampling loads. | Keep the cardinal dilation as a bounded bake-side improvement; do not claim it closes no-wrong-side or zero-leak gates. |
| Diagonal bake dilation | REJECTED | Adding one-pixel diagonal taps reduced `wrongSideEscapedCount` to `3` and residual leak to `0.9353`, but opened `receiverSurface.cpuRenderAgreement` and `shContribution.noBakedMixedColorRisk`. | Reject because it trades leak reduction for overreach/mixed-color risk; this is not a valid proof promotion. |
| Visibility angular resolution sweep | REJECTED | 16px visibility-depth resolution improved residual leak to `0.908` but opened directional suppression (`0.2855`); 12px improved residual to `0.9274` but still opened directional suppression (`0.0812`); 10px preserved gate shape but left residual leak at `0.942`. | Keep 8px for this patch; increasing angular resolution is a real tradeoff, not a clean proof fix. |
| Visibility representation interface | SUPPORTED | Phase 57 defines the representation seam as coupled angular resolution, moment filtering, bias policy, border/gutter behavior, GPU-resident visibility decision, compact diagnostic facts, and over-occlusion guard facts. It records that the current 8px exact-bin no-hit misses survive cardinal bake dilation and that raw 12px/16px sweeps over-occlude by opening directional suppression. | Do not prototype until a candidate names the full representation policy; naive adjacent fallback, raw resolution bumps, diagonal overreach, threshold tuning, and scalar/WebGL-as-truth framing remain rejected. |
| Phase 57 representation candidate facts | SUPPORTED | The current cardinal-dilated moment representation now emits compact facts consumed by `visibilityWeighting.noWrongSideEscape`: angular resolution, effective bias, moment filter, border policy, moment hit/no-hit coverage, wrong-side escape count, and wrong-minus-correct suppression. Focused Cornell proof passed 33 checks and kept 14 supported / 2 open gates with `angularResolution = 8`, `effectiveBias = 0.02`, moment hit/no-hit counts `5/11`, and `wrongSideEscapedCount = 4`. | Promote the fact contract, not the candidate as a fix. The current representation remains rejected for no-wrong-side promotion because the existing gate stays open; next work needs a stronger representation or classification policy. |
| Receiver-surface raw gate evidence | SUPPORTED | Receiver-surface open gate now reports compact raw inputs plus derived delta: `renderSurfaceWrongRatio = 0.8219`, `surfaceRuntimeWrongRatioMax = 0.1263`, `surfaceCpuRenderDelta = 0.6956`. | Treat the receiver-surface gate as render/surface-capture disagreement against a low CPU runtime mirror; do not retune SH diagnostics without source evidence. |
| Final proof package framing | SUPPORTED | Roadmap now states the final proof claim, non-claims, and the remaining need to rerun focused Cornell proof artifacts after the receiver-surface and SH-risk source fixes. | Do not add more cleanup phases; refresh eval proof or package the claim with explicit open diagnostics. |
| GPU-debug narrative cleanup | SUPPORTED | Receiver GPU-debug now emits raw unavailable/surface facts only; no focused debug gate is introduced. | Keep raw-only until a real debug verifier exists. |
| Visibility moment verdict ownership | SUPPORTED | Moment inspection now emits raw availability/mode/byte/sample facts; `visibility.momentReadback` derives the support verdict. | Keep. |
| Visibility status payload cleanup | SUPPORTED | The unused `deriveVisibilityProofStatus()` wrapper and its `visibilityLabel`/`visibilityStatus`/`ddgiStatus` payload are gone; moment-backed validation uses the raw `isMomentBackedVisibility()` predicate only. | Keep visibility/DDGI narrative status out of proof facts unless a gate directly consumes it. |
| Leak proof verdict ownership | SUPPORTED | Leak proof facts now emit raw sealed-wall baseline/candidate rows; proof gates derive linear pre-tone masked leak improvement plus correct-bounce preservation thresholds. | Keep tone-mapped wrong-side improvements as raw derivable facts only, not promotion blockers. |
| Leak evidence artifact visibility | SUPPORTED | Artifact writer now persists compact `leak-proof-facts.json` with sealed-wall baseline/candidate raw leak rows next to `proof-summary.json`; validation guards finite raw leak/bounce metrics. | Keep raw baseline/candidate evidence auditable without bloating proof-summary gates or reintroducing nested metric snapshots. |
| Ground-truth leak oracle | SUPPORTED | Leak artifacts now carry `wrongSideLeakTarget: 0`, and proof gates derive residual pre-tone masked wrong-side leak against that physical sealed-wall oracle separately from baseline-vs-candidate improvement. | Keep baseline as a leaky comparator, not ground truth; residual leak remains visible until fixed. |
| Residual leak classification | SUPPORTED | Current focused Cornell proof keeps `sealedWall.preToneMaskedWrongSideResidualLeakRatio = 0.9377` after receiver-scoped no-wrong-side escape support. Residual attribution now points to rendered masked receiver energy after compatibility shaping, not unresolved wrong-side probe escape. | Further reduction needs a broader receiver descriptor, material response, or visibility/probe representation seam, not threshold tuning or scalar-baseline truth. |
| Baseline modernization framing | SUPPORTED | Upstream three.js issue/PR history (`#16228`, `#18371`) describes the old gap as probe-volume interpolation for objects/materials, grid-friendly GPU sampling, HDR probe data, and clearer separation between probes/irradiance and scene lights. | Treat the old scalar/LightProbe path as a comparator and product-gap source, not as a ground-truth target. |
| External leak-mitigation research | SUPPORTED | DDGI/RTXGI sources frame light leaking as a visibility/geometry problem; Morgan McGuire's DDGI overview separates low-frequency irradiance from 16x16 visibility/distance tiles; Unity APV uses validity, virtual offset, dilation, rendering layers, density, and probe adjustment volumes; Flax documents depth plus Chebyshev visibility weighting and relocation/classification debugging. Practitioner Reddit threads show the same leak concerns, but are corroborating signals rather than primary proof. | Treat the current 8px visibility-depth resolution as a measured representation limitation. Next algorithmic work should target visibility representation or classification/relocation, not threshold tuning, proof prose, or naive adjacent-bin borrowing. |
| Healthy trim direction | SUPPORTED | Adjacent-cross and angular-resolution sweeps are rejected; proof gates already record why. Remaining useful evidence is compact raw gate input plus source invariants. | Remove speculative failed-path payloads and avoid new diagnostics unless they directly feed a gate, artifact contract, or public example. |
| Rejected adjacent-fallback payload trim | SUPPORTED | The rejected adjacent-fallback evaluator and `adjacentFallback*` counters were removed from the visibility study, proof facts, runner assertions, and proof-gate evidence. | Keep failed-rescue details in the ledger/roadmap only; do not reintroduce fallback payloads unless a new algorithmic candidate is being actively evaluated. |
| Legacy exact-bin taxonomy prune | SUPPORTED | Phase 105 removes old weak-crush/exact-bin no-hit localization subcounts from the live visibility study, proof-gate evidence, and runner assertions. Source invariants now forbid those stale fields in the live visibility/proof interface. Focused Cornell WebGPU proof smoke still passes with `OPEN`, 15 supported / 1 open. | Keep exact-bin and adjacent-hit evidence as historical rejection context only; the current open gate is residual masked receiver energy after receiver-scoped ownership shaping. |
| Residual boundary policy SDD | SUPPORTED | Phase 105 defines the next eligible candidate as a GPU-resident receiver residual boundary policy using authored/setup classification facts through `receiverBoundaryLayerMask` and `receiverBoundaryWeight`. | Prototype only if the source is generic and GPU-resident. Promotion requires lowering `sealedWall.preToneMaskedWrongSideResidualLeakRatio` while preserving no-wrong-side escape, directional suppression, receiver-surface agreement, SH-risk, correct bounce, runtime readiness, projection parity, moment readback, residual honesty, and compact proof shape. |
| Coherent biased-query prototype | REJECTED | Using the normal/view-biased sample point for probe direction, compatibility kernel, and visibility distance passed smoke but regressed proof to 14 supported / 2 open. Baseline-relative improvement opened at `-0.0243`, residual worsened to `1.0243`, and candidate pre-tone masked wrong-side ratio became `0.8961` versus baseline `0.8748`. | Trimmed from runtime and proof mirror. Keep the current split-sample contract; the remaining residual needs explicit receiver-boundary/discontinuity classification or geometry state, not global biased-query ownership. |
| Sixteenstudio private comparison branch | SUPPORTED | Public commit `e8d975a347e0d1e4756c8d51fdbe1887b1f0b210` from `sixteenstudio/three.js feat/webgpu-lightprobes-sponza` was pushed to private fork branch `gpu/sixteenstudio-lightprobes-sponza`. | Use as WGPU SH atlas / product API control. Do not treat it as leak-control target because it lacks visibility moments, receiver masks, probe ownership metadata, relocation/dilation, and residual proof gates. |
| Sixteenstudio control diff | SUPPORTED | Phase 108 compares atlas, node, and demo shape. The control uses one packed RGBA 3D SH atlas with copied Z padding between seven SH sub-volumes, a dedicated `LightProbeGridNode`, normal-offset sampling, and a Sponza demo with rebake/helper/dynamic-object integration. It does not expose visibility moments, receiver-boundary metadata, or residual proof gates. | Adopt product/API lessons only: compact base SH atlas path, clearer node seam, and demo integration pressure. Do not merge the sixteenstudio runtime into the SDD branch as a residual-leak fix. |
| Literature-backed roadmap revision | SUPPORTED | Phase 107 maps sources to decisions: RTXGI/DDGI and Flax support probe visibility/distance; Unity APV supports rendering-layer masks, validity, dilation, virtual offset, density, and adjustment volumes; light-field probes support per-texel visibility; SDF and edge-aware/limiter analogies support explicit discontinuity classification. | Next implementation must add source-backed receiver/probe classification or geometry state before touching residual math. Keep pure SH interpolation as the base irradiance path, not the final leak-control answer. |
| Receiver boundary source adapter SDD | SUPPORTED | Phase 109 defines two generic receiver boundary/class sources: authored geometry/material GPU node data and setup-authored region/layer ownership metadata resolved before runtime upload. It rejects proof pixels, CPU readback, scalar/WebGL truth, brightness validity, Cornell divider coordinates, threshold tuning, and old adjacent-fallback payloads. | A residual candidate may proceed only from source-backed GPU metadata. Diagnostics may emit compact raw source type, selected receiver ratio, same-side preservation, cross-side exclusion, residual input ratios, and over-occlusion guard inputs; proof gates own verdicts. |
| GPU metadata layout SDD | SUPPORTED | Phase 110 keeps probe ownership in existing `probeMeta.b`, uses receiver node/value inputs for `receiverLayerMask`, `receiverBoundaryLayerMask`, and `receiverBoundaryWeight`, preserves default behavior when boundary metadata is absent, and defers new compact GPU buffers/textures until a many-class source needs them. | Prototype remains held until a Phase 109 source is wired through this layout. Runtime must stay GPU-resident, raw-only, and classification-before-interpolation; do not add CPU readback or irradiance darkening. |
| Residual candidate prototype audit | HELD | Phase 111 audits the available runtime seam and rejects a shallow wrapper prototype. The existing Cornell equivalence path already passes `receiverBoundaryLayerMask` plus `receiverBoundaryWeight = 1`, keeps the supported guards, and still leaves `sealedWall.preToneMaskedWrongSideResidualLeakRatio = 0.9377`. | Do not add a helper that only renames the existing descriptor object. The next candidate must add represented receiver/material/geometry state before interpolation and prove residual movement through existing gates. |
| Promotion or trim after source/layout audit | HELD | Phase 112 records no promotable residual candidate after the sixteenstudio control diff, source adapter SDD, and GPU layout SDD. The proof package remains `OPEN`, 15 supported / 1 open. | Keep the bounded proof claim. Promotion requires residual below `0.9377` without opening no-wrong-side escape, directional suppression, receiver-surface agreement, SH-risk, correct bounce, runtime readiness, projection parity, moment readback, or compact proof shape. |
| Ultraplan architecture alignment | SUPPORTED | Roadmap and design now align the image-backed plan into five lanes: visibility representation, probe classification/relocation, bias/resolution coupling, product API shape, and proof compactness. | Start Phase 57 with visibility representation SDD; block raw resolution bumps, adjacent fallback revival, and baseline-as-truth promotion. |
| Matrix runner predicates | SUPPORTED | Matrix smoke assertions now validate compact probe occupancy, sealed-wall leak-proof fixture facts, leak-proof row facts, and restoration facts through named local predicates guarded by source invariants. | Keep matrix runner checks predicate-owned; avoid rebuilding proof-gate derivations in the runner. |
| Projection profiler raw facts | SUPPORTED | `inspectComputeProjectionProfiling()` now emits timing sources, static work, medians, and backend/fallback facts without diagnostic status/policy strings. | Keep profiler diagnostic non-gated and raw. |
| Projection runtime parity verdict ownership | SUPPORTED | Runtime parity now emits raw backend, coefficient, atlas, and fallback facts; `projection.runtimeParity` derives the verdict. | Keep `tolerancePass` out of runtime parity facts. |
| Core runtime parity/profile predicates | SUPPORTED | Core smoke assertions now validate compute projection runtime parity facts, parity support facts, profiling selector facts, and compact timing facts through named local predicates guarded by source invariants. | Keep parity/profile runner checks predicate-owned; proof gates keep verdict ownership. |
| Projection oracle raw facts | SUPPORTED | Projection candidate, adapter fallback, parity contract, and atlas repack oracles now emit raw paths/capabilities/thresholds/deltas instead of proof-only status strings. | Keep. |
| Projection oracle runner predicates | SUPPORTED | Core smoke assertions now validate projection parity contract, compute candidate, adapter fallback, and atlas repack raw facts through named local predicates guarded by source invariants. | Keep projection-oracle runner checks predicate-owned; avoid growing inline assertion blobs. |
| Artifact pressure verdict ownership | SUPPORTED | Artifact pressure helpers now emit raw black-tail/luminance/contrast facts; proof validation owns pressure and bounded thresholds. | Keep. |
| Density fixture proof ownership | SUPPORTED | Proof validation now derives the shared WebGPU/WebGL density budget and same-fixture checks with named predicates instead of repeated inline field chains. | Keep density artifact rows raw; keep fixture-equivalence verdicts in validation. |
| Density row validation predicates | SUPPORTED | Proof validation now checks density reference, shadowless, crisp-shadow row facts, crisp-shadow reference comparison, damped fixture-preservation, and damped math-action rows through named predicates, and fixes the damped assertion message comma bug. | Keep density row policy in validation predicates; keep artifact rows raw. |
| Low-res validation predicates | SUPPORTED | Proof validation now checks low-res shared probe intensity, SH band isolation, green-bounce preservation, red-bias improvement, and dark-tail bounds through named predicates. | Keep low-res comparison policy in validation predicates; keep artifact rows raw. |
| Proof summary validation predicates | SUPPORTED | Proof summary validation now checks bounded gate count, unique and required ids, compact gate shape, required supported gates, and byte budget through named predicates guarded by source invariants. | Keep proof-summary contract checks predicate-owned; do not rebuild giant proof reports. |
| Artifact snapshot narrative cleanup | SUPPORTED | WebGPU snapshots no longer echo `proofRole`/`antiRingingPolicy`; validation derives the label-owned role and anti-ringing expectations from compact row facts, while the WebGL reference keeps its explicit same-class reference role. | Keep generated WebGPU artifact rows raw and non-narrative. |
| Projection fallback narrative cleanup | SUPPORTED | Adapter fallback scenarios now emit raw runtime guard, capability, selected path, and fallback facts without prose `reason`. | Keep. |
| Remaining verdict ownership | SUPPORTED | Final scan leaves only operational runtime/smoke statuses and proof-gate verdict status construction; harness diagnostics no longer carry the targeted narrative/verdict payloads. | Keep. |
| SDD/OpenSpec persistence | SUPPORTED | Change folder records proposal, claim, design, tasks. | Continue. |
| Receiver-surface center capture evidence | SUPPORTED | Center receiver render capture reports `renderSurfaceCenterWrongRatio = 0.9314`, worse than broad surface `0.8219`, while CPU runtime mirror stays `0.1263`. | The receiver-surface mismatch is not explained by projected surface-region overreach; keep the gate open as render/runtime diagnostic disagreement. |
| Receiver-surface runtime-irradiance evidence | SUPPORTED | Unlit receiver-center render using `probeGrid.createIrradianceNode()` reports `renderIrradianceCenterWrongRatio = 0.5882` versus CPU center mirror `0.0635`, delta `0.5247`. | The mismatch exists before standard-material shading; keep investigating CPU mirror/runtime-node disagreement rather than surface-region capture. |
| Receiver-surface linear-irradiance gate fix | SUPPORTED | `receiverSurface.cpuRenderAgreement` now compares linear unlit runtime-irradiance center render to CPU center runtime mirror; current proof summary keeps receiver-surface supported after receiver-scoped ownership. | Display-mapped standard-material surface ratios remain diagnostic evidence only and must not drive CPU/render agreement. |
| Final bounded proof package | SUPPORTED | Roadmap final notes list 15 supported / 1 open gate, exact baseline-vs-candidate leak values (`0.8626` -> `0.8089`), correct-bounce preservation, receiver-scoped no-wrong-side support, and residual `0.9377` against the zero-leak oracle. | Present this as bounded leak improvement and receiver-scoped escape closure, not zero-leak DDGI/GI. |
| Probe classification contract | SUPPORTED | Phase 58 defines valid, invalid, interior, exterior, occluding, and relocated aggregate categories. The harness emits `classificationPolicy = 'solid-occupancy-validity'`, `relocationPolicy = 'none'`, compact counts, and no per-probe rows or brightness-derived validity. | Treat this as the current GPU-validity classification contract, not as a promoted wall-relocation fix. The wrong-side escape gate remains owned by visibility representation evidence. |
| Bias/resolution coupling closeout | SUPPORTED | Phase 59 keeps `VISIBILITY_DEPTH_RESOLUTION = 8` because no candidate currently defines a resolution-scaled moment/bias policy that improves no-hit escape evidence while preserving directional suppression and the other supported gates. | Do not rerun naked 8/10/12/16 sweeps. A future sweep must define moment filtering, bias, border/gutter behavior, and over-occlusion guard facts before measurement. |
| Product API shape | SUPPORTED | Phase 60 classifies product controls as GPU-resident grid setup, bake inputs, `probeValidity`, leak-reduction mode, band/intensity controls, and object/material irradiance nodes. Harness inspections and proof rows remain proof scoped. | Keep scalar/WebGL as same-class reference or leaky comparator, never oracle. Do not modernize this module toward scene-global mutable `LightProbe` state. |
| Proof compactness closeout | SUPPORTED | Phase 61 source invariants guard against per-probe classification rows, relocated-probe payloads, brightness-derived validity, adjacent-fallback payloads, and diagnostic verdict strings. | Keep rejected paths in roadmap/ledger only; runtime, diagnostics, and runner contracts keep compact raw facts consumed by gates, artifact contracts, or examples. |
| Open-gate SDD plan | SUPPORTED | Phase 62 couples the remaining local escape gate and scene-level residual leak gate while preserving separate verdict ownership. The next valid candidate is border-aware visibility representation with resolution-scaled moment/bias policy and explicit over-occlusion guards. | Start Phase 63 by specifying the representation contract before prototyping. Do not reopen threshold tuning, naive adjacent fallback, raw resolution bumps, or scalar/WebGL-as-truth framing. |
| Forward roadmap constraint | SUPPORTED | Phases 63-70 now define a finite ordered path: border-aware visibility representation, verdict/trim, wall-aware classification if needed, coupled resolution only under policy, residual oracle closeout, product API hardening, final proof package, and archive/continue decision. | Do not add new algorithmic phases unless earlier gate evidence requires them. Every failed candidate must be trimmed before the next lane starts. |
| Border-continuous oct UV candidate | REJECTED | Phase 63 candidate A removed clamp-before-decode for bake-side cardinal taps while keeping 8px angular resolution, runtime exact-bin lookup, and `effectiveBias = 0.02`. Focused Cornell proof smoke passed but `wrongSideEscapedCount` remained `4` and residual leak remained `0.9377`; no local escape class improved. | Candidate was trimmed from runtime/proof interfaces. Continue to Phase 65 wall-aware classification/relocation only if the next policy is real sampling metadata, not brightness validity or fixture-only proof behavior. |
| Wall-aware classification runtime seam | DEFERRED | Phase 65 found that runtime already has GPU probe layer metadata and layer compatibility, but the receiver mask is grid-scoped and `createIrradianceNode()` has no receiver-specific sampling parameter. The sealed-wall proof needs different masks for left/right receivers in the same render, so using the current uniform would be scene-global mutable state. | Do not prototype wall-aware classification until receiver-specific layer/mask sampling metadata is designed. Brightness validity, fixture-only divider checks, and global receiver-mask mutation stay rejected. |
| Remaining phase closeout | SUPPORTED | Phases 66-70 are closed from evidence: no coupled resolution sweep without a promoted policy, residual leak remains open at `0.9377` against zero, and the later receiver-specific layer/mask API moved no-wrong-side escape to supported. | Continue only with new source-backed evidence for the remaining residual or publish the bounded claim with the residual gate intact. |
| Source-backed open-gate reframe | SUPPORTED | DDGI/RTXGI sources frame leak control as per-probe visibility/distance plus statistical occlusion, self-shadow bias, probe state, relocation, classification, and volume blending. Unity APV exposes validity, dilation, virtual offset, renderer layers, density, and adjustment volumes. Flax DDGI documents low-resolution depth plus Chebyshev visibility and probe relocation debugging. | Treat the open-gate history as visibility/probe-shaping evidence and the current residual as a receiver/material/visibility representation problem. Do not reopen threshold tuning, naive adjacent fallback, naked resolution bumps, or scalar/WebGL-as-truth framing. |
| Receiver-scoped probe mask API direction | PLANNED | Current runtime has GPU probe layer metadata and compatibility logic, but receiver mask input is grid scoped. The sealed-wall proof needs distinct left/right receiver masks in one render, and product API should express that as receiver/material/node sampling metadata. | Start Phase 72 by designing `createIrradianceNode( { receiverLayerMask } )` or equivalent. Prototype only after metadata packing, memory policy, compact facts, and over-occlusion guards are specified. |
| Receiver-scoped probe mask API seam | SUPPORTED | `createIrradianceNode( options = {} )` and `createLightsNode( sceneLights = [], options = {} )` now accept a receiver-scoped `receiverLayerMask` node/value. Manual weighted sampling resolves it to a GPU uint node and applies it to the existing `probeMeta.b` compatibility test; the default path still uses the grid-level mask. | Keep the API seam. Do not claim gate movement until a wall-shaping policy assigns probe layers, emits compact aggregate facts, and passes over-occlusion guards. |
| Probe layer metadata seam | SUPPORTED | Optional `probeLayerMasks` are validated as 24-bit integer masks and packed into the existing probe metadata texture's `probeMeta.b` channel, preserving the zero-extra-texture memory policy for the first receiver-scoped mask candidate. | Keep metadata GPU-resident and generic. The next slice must define compact shaping facts before using left/right wall masks in the harness. |
| Cornell side-mask metadata | SUPPORTED | The Cornell harness assigns default-compatible side masks: left probes include the default bit plus a left bit, and right probes include the default bit plus a right bit. Receivers using default mask `1` still match all probes. | Keep as metadata setup only. Do not use side masks for proof promotion until receiver-specific shaping facts and over-occlusion guards are implemented. |
| Receiver-scoped shaping preview facts | SUPPORTED | Visibility-weighting diagnostics now emit compact aggregate facts for the side-mask policy: mask modes, compatible/incompatible counts, excluded wrong-side probes, preserved correct-side probes, shaped wrong-side escape count, and preservation/exclusion ratios. Proof facts carry this evidence under `visibilityWeighting` without changing the existing gate verdict. | Use these facts as the precondition for applying receiver-specific masks in render/proof. Do not promote until actual masked runtime proof preserves suppression, surface, SH-risk, bounce, and residual honesty. |
| Receiver-scoped mask candidate verdict | SUPPORTED | Focused Cornell WebGPU proof smoke passed 33 checks with receiver-specific left/right masks applied in render/proof. Proof summary improves to `OPEN` with 15 supported / 1 open gate: `visibilityWeighting.noWrongSideEscape` is supported through receiver-scoped shaping facts, while `sealedWall.preToneMaskedWrongSideResidualLeakRatio` remains open at `0.9377` against zero. | Promote the local receiver-scoped mask contract. Do not claim zero leak; continue residual work separately and preserve over-occlusion guards. |
| Residual leak attribution SDD | SUPPORTED | Leak proof facts now emit compact residual attribution ratios. Focused Cornell WebGPU smoke passed 33 checks and kept 15 supported / 1 open gate. Attribution reports screen-region `1.0053`, surface `0.9359`, surface-center `0.9666`, masked-visible `1.0012`, and pre-tone masked `0.9377`, confirming the remaining gate is rendered receiver-region residual energy rather than a wrong-side probe escape. | Do not prototype from constants. The next candidate must explain the masked-visible residual source while preserving the Phase 73 receiver-scoped mask contract and all over-occlusion guards. |
| Rendered receiver residual source SDD | SUPPORTED | Phase 75 source facts add candidate irradiance-only center ratios. Focused Cornell WebGPU smoke passed 33 checks and kept 15 supported / 1 open gate. Candidate display irradiance center wrong-ratio is `0.6241`, candidate linear irradiance center wrong-ratio is `0`, and candidate linear-irradiance-to-masked-visible ratio is `0`, while masked-visible residual remains `1.0012`. | Reject "center linear irradiance is still wrong" as the residual explanation. Next candidate should focus on receiver/material response, visible-surface coverage, or region sampling geometry before changing visibility math or probe compatibility. |
| Visible-surface / material response SDD | SUPPORTED | Phase 76 fact pass adds visible-pixel mask geometry and material/output ratios. Focused Cornell WebGPU smoke passed 33 checks and kept 15 supported / 1 open gate. Candidate masked visible pixel count is `5557`, masked visible pixel ratio is `0.0138`, masked-visible-to-surface-center ratio is `0.7867`, and pre-tone-masked-to-masked-visible ratio is `1.1102`. | Reject broad mask coverage as the primary explanation. Next source lane should isolate material/output mapping and receiver surface sampling before changing probe visibility or compatibility. |
| Material/output mapping attribution SDD | SUPPORTED | Phase 77 fact pass adds irradiance-only masked-visible ratios under the same receiver mask. Focused Cornell WebGPU smoke passed 33 checks and kept 15 supported / 1 open gate. Candidate display irradiance masked wrong-ratio is `0.7244`, candidate linear irradiance masked wrong-ratio is `0.5037`, and candidate masked-irradiance-to-masked-visible ratio is `0.6913`. | Treat output/material response as the next source class. Do not reopen visibility representation or receiver compatibility. |
| Material albedo / BRDF attribution SDD | SUPPORTED | Phase 78 fact pass adds actual receiver material facts. Focused Cornell WebGPU smoke passed 33 checks and kept 15 supported / 1 open gate. Candidate material type is `standard`, receiver albedo wrong-side ratio is `1.0655`, roughness is `0.82`, metalness is `0`, and pre-tone-masked-to-albedo ratio is `0.7592`. | Reject albedo as the primary residual amplifier. Continue through BRDF / surface-normal transport attribution under the same receiver mask. |
| BRDF / surface-normal transport attribution SDD | SUPPORTED | Phase 79 fact pass adds Lambert material masked ratios under the same receiver mask. Focused Cornell WebGPU smoke passed 33 checks and kept 15 supported / 1 open gate. Lambert display masked wrong-ratio is `0.7286`, Lambert linear masked wrong-ratio is `0.6565`, linear-Lambert-to-linear-irradiance ratio is `1.3034`, and pre-tone-masked-to-linear-Lambert ratio is `1.2321`. | BRDF/material response contributes but is not the whole residual. Continue through receiver normal / surface sampling attribution before any prototype. |
| Receiver normal / surface sampling attribution SDD | SUPPORTED | Phase 80 fact pass adds receiver normal and surface coverage facts. Focused Cornell WebGPU smoke passed 33 checks after one browser target-close rerun and kept 15 supported / 1 open gate. Minimum camera-dot-CPU-normal is `0.7999`, receiver surface region area ratio is `0.0136`, and masked visible pixel ratio is `0.0138`. | Reject normal convention and mask/surface coverage mismatch as primary residual explanations. Continue through receiver surface spatial distribution before any prototype. |
| Receiver surface spatial distribution SDD | SUPPORTED | Phase 81 fact pass adds near-divider receiver-edge ratios. Focused Cornell WebGPU smoke passed 33 checks and kept 15 supported / 1 open gate. Candidate near-divider edge wrong-ratio is `0.8729`, near-divider-edge-to-surface-center ratio is `0.9425`, and near-divider-edge-to-masked-visible ratio is `1.1981`. | Classify residual as spatially concentrated near the divider-facing receiver edge. Continue only with a generic surface/discontinuity policy, not a fixture hack. |
| Near-divider surface distribution candidate SDD | SUPPORTED | Phase 82 defines the representation contract for generic receiver/material/node-scoped surface-discontinuity metadata. The policy must stay GPU-resident, reuse the receiver mask seam where possible, preserve default behavior, and report only compact aggregate proof facts. | Do not prototype yet. Next specify the concrete receiver descriptor layout and GPU weighting rule, or record that the current runtime seam needs a broader receiver-surface metadata API. |
| Receiver descriptor layout SDD | SUPPORTED | Phase 83 records that the existing `receiverLayerMask` node/value seam can express a discrete dynamic mask: interior samples use an interior-compatible mask, boundary-near samples use a stricter boundary-compatible mask, and probe ownership stays in `probeMeta.b`. | Prototype only if boundary-safe probe ownership can be authored as generic bake metadata. If it collapses to Cornell divider logic, defer instead of coding it. |
| Boundary-safe probe ownership authoring SDD | SUPPORTED | Phase 84 defines boundary-safe probe classes as setup/bake-authored metadata consumed through `probeMeta.b` and receiver-supplied GPU mask nodes. The runtime does not infer wall ownership from proof rows, brightness, CPU readback, or scalar/WebGL comparison. | Prototype remains deferred until authored boundary-safe classes and compact aggregate proof facts are defined without Cornell-only semantics. |
| Boundary ownership proof-fact contract SDD | SUPPORTED | Phase 85 defines the compact aggregate facts required before any authored boundary-safe ownership prototype: policy id, mask modes, probe class counts, boundary-selected receiver ratio, contribution ratios, wrong-side exclusion, correct-side preservation, residual input, spatial residual ratio, and correct-bounce energy. | Keep verdicts in proof gates. Prototype only after these facts are tied to generic authored ownership metadata, not Cornell divider coordinates or rejected-path payloads. |
| Generic boundary ownership assignment SDD | SUPPORTED | Phase 86 defines allowed assignment sources from source-backed engine patterns: author layers, bake/setup geometry classification, authored probe invalidity/relocation/dilation state, and receiver GPU mask inputs. The current Cornell side split remains valid region ownership but is not a generic boundary-safe residual prototype. | Next implementation requires a bake/setup assignment adapter that derives stable ownership bits from product-visible layers or geometry classification and emits the Phase 85 aggregate facts. |
| Probe ownership assignment adapter SDD | DEFERRED | Phase 87 defines the setup/bake-time adapter seam that can produce generic `probeLayerMasks`, but the only concrete current rule source is Cornell's proof-scoped side split. A reusable adapter with one fixture rule would fail the deletion test. | Keep Cornell `createProbeLayerMaskData()` proof-scoped. Product authoring shape must provide at least two non-identical generic rule sources before prototype. |
| Product authoring shape gap SDD | SUPPORTED | Phase 88 records the blocker for a generic boundary-safe residual prototype: the product must choose ownership authoring semantics. Valid shapes are layer ownership, region ownership, geometry classification, and probe-state ownership from invalidity/relocation/dilation. | Residual gate stays open. This is a product authoring semantics gap, not a runtime GPU-residency or proof infrastructure blocker. |
| Layer ownership product shape SDD | SUPPORTED | Phase 89 selects layer ownership as the first product-facing authoring shape because three.js already has `Object3D.layers` and the `Layers` bitmask vocabulary. Probe ownership may map those bits into the current 24-bit `probeMeta.b` range, while receivers consume GPU masks through `receiverLayerMask`. | This selects one authoring shape only. Adapter prototype remains deferred until a second distinct setup/bake classification source is selected. |
| Region ownership product shape SDD | SUPPORTED | Phase 90 selects region ownership as the second setup/bake classification source. Region rules use authored world-space `Box3` volumes over deterministic probe positions and output the same 24-bit masks consumed by `probeLayerMasks`. | Layer plus region ownership satisfy the two-source design requirement. Next specify the concrete adapter interface and Phase 85 aggregate diagnostics before prototype. |
| Probe ownership assignment adapter interface SDD | SUPPORTED | Phase 91 specifies the concrete setup/bake adapter interface: grid bounds/resolution/default mask, layer and region rule schemas, `probeLayerMasks` output, compact `assignmentFacts`, and validation for mask range, finite boxes, unique rule names, and overlaps. | Prototype remains held until implementation can map `assignmentFacts` to Phase 85 proof facts without widening runtime sampling or claiming residual movement without focused proof evidence. |
| Assignment facts to proof facts mapping SDD | SUPPORTED | Phase 92 maps adapter-owned `assignmentFacts` into Phase 85 names and separates fields the adapter cannot own: receiver sample selection, contribution ratios, wrong-side exclusion, correct-side preservation, residual ratio, spatial ratio, and correct-bounce energy. | Prototype still held. Next define compact proof-harness facts for the runtime/proof-owned fields before implementation. |
| Boundary ownership proof harness facts SDD | SUPPORTED | Phase 93 defines `boundaryOwnershipShaping` as the compact proof-harness fact object for receiver mask mode, boundary-selected receiver sample ratio, contribution ratios, wrong-side exclusion, correct-side preservation, and preservation/exclusion ratios. Residual, spatial, and correct-bounce ratios remain owned by existing proof paths. | Prototype may proceed only if it consumes adapter `assignmentFacts`, emits compact aggregate fields only, and keeps verdict ownership in proof gates. |
| Layer / region rule binding SDD | SUPPORTED | Phase 94 records that layer rules define ownership classes while region rules select probes. Region rules may bind to a layer rule by name or assign an ownership mask directly; assignment facts must report bound and unbound layer rules so two arrays cannot masquerade as two classifiers. Source invariants now protect the SDD binding text. | The adapter prototype may proceed only if it implements this binding contract without widening runtime sampling. |
| Probe ownership assignment adapter prototype | SUPPORTED | Phase 95 adds setup-only `LightProbeGridGPUProbeOwnership`, producing `probeLayerMasks` and compact `assignmentFacts` from layer/region rules. Cornell side ownership now routes through the adapter. Runtime sampling still consumes only `probeMeta.b` and receiver GPU masks. | No residual gate movement is claimed. Next add proof-harness `boundaryOwnershipShaping` only if it consumes adapter facts compactly and preserves existing residual/bounce owners. |
| Boundary ownership shaping facts prototype | SUPPORTED | Phase 96 keeps Cornell `assignmentFacts` in setup context and emits compact `boundaryOwnershipShaping` from adapter assignment coverage plus receiver-scoped preservation/exclusion aggregates. Proof facts carry this evidence under `visibilityWeighting` without adding residual verdicts or runtime readback. Focused Cornell smoke still reports `OPEN` with 15 supported / 1 open gate; residual remains `0.9377`. | Residual gate remains separate; the next candidate must move the zero-oracle residual or classify why it cannot move under the current surface/discontinuity policy. |
| Residual decision boundary SDD | SUPPORTED | Phase 97 classifies the remaining residual as rendered masked receiver energy after compatibility shaping. Current evidence: center linear irradiance `0`, masked linear irradiance `0.5037`, Lambert linear masked `0.6565`, near-divider edge to masked-visible `1.1981`, and pre-tone masked to masked-visible `1.1102`. Wrong-side escape, center contamination, albedo-only amplification, broad mask mismatch, and receiver-normal convention are rejected explanations. | Prototype remains held. The next code candidate must name a GPU-resident receiver descriptor, material response policy, or stronger visibility/probe representation seam with compact guard facts before changing runtime math. |
| Receiver discontinuity descriptor contract SDD | SUPPORTED | Phase 98 defines the next candidate interface as existing `receiverLayerMask` plus optional GPU-resident `receiverBoundaryLayerMask` and `receiverBoundaryWeight`. It keeps probe ownership in `probeMeta.b`, preserves default behavior, limits diagnostics to compact aggregate facts, and requires all over-occlusion guards to remain supported. | Prototype was held until Phase 99/100 supplied a generic authored source and deterministic selection rule. Cornell divider coordinates, residual-derived descriptors, CPU readback, and scalar/WebGL truth remain rejected. |
| Authored boundary weight source SDD | SUPPORTED | Phase 99 selects caller-authored GPU node/value metadata as the minimum generic source for `receiverBoundaryWeight`. Accepted sources include constants, uniform-like nodes, attributes, material-node expressions, and setup-authored receiver metadata resolved to GPU floats; rejected sources include proof residuals, pixels, visibility readbacks, scalar/WebGL comparison, CPU readback, Cornell divider coordinates in runtime, brightness classification, and threshold tuning. | Prototype was held until Phase 100 specified native WGSL mask selection as deterministic classification, not irradiance energy scaling. |
| WGSL boundary mask selection API prototype | SUPPORTED | Phase 100 adds default-preserving receiver boundary descriptor options to runtime sampling: `receiverBoundaryLayerMask` defaults to the resolved receiver mask, `receiverBoundaryWeight` defaults to `0` and clamps to `[0, 1]`, `effectiveReceiverLayerMask` is selected by a native WGSL helper using `select( receiverLayerMask, receiverBoundaryLayerMask, receiverBoundaryWeight >= 0.5 )`, and probe/receiver compatibility is converted through a second native WGSL helper using `select( 0.0, 1.0, ( probeLayerMask & receiverLayerMask ) != 0u )`. | No residual movement is claimed. The selectors are classification only; they do not scale irradiance energy or alter visibility moments, resolution, bias, SH sampling, or proof-gate math. |
| Boundary descriptor harness equivalence prototype | SUPPORTED | Phase 101 routes Cornell proof-only receiver masks through `receiverBoundaryLayerMask` plus `receiverBoundaryWeight = 1` using a local descriptor helper. Unlit irradiance capture, Lambert response capture, and receiver material light-node setup now exercise the WGSL selector while preserving the already-promoted receiver-scoped ownership behavior. Focused Cornell WebGPU smoke passed 33 checks and stayed `OPEN` with 15 supported / 1 open; residual remains `0.9377`. | This is equivalence coverage only. It does not infer discontinuities, use Cornell divider coordinates in runtime, or claim residual movement. |
| Final package reconciliation | SUPPORTED | Phase 102 records the latest focused Cornell smoke as the authoritative current state: `OPEN`, 15 supported / 1 open. The no-wrong-side and directional-suppression gates are supported after WGSL descriptor and compatibility routing; the only remaining open gate is `sealedWall.preToneMaskedWrongSideResidualLeakRatio = 0.9377` against the zero-leak oracle. | Older 13/3 and 14/2 proof summaries remain historical evidence for rejected or superseded paths. Do not close the package by hiding the residual gate. |
| Checklist guard reconciliation | SUPPORTED | Phase 103 closes stale unchecked prototype-guard boxes as explicit defer/superseded decisions. `tasks.md` now has no unchecked boxes, while the proof summary remains `OPEN` with 15 supported / 1 open. | A fully checked task list is not a zero-leak claim. The residual gate remains open and must stay visible in the final package. |
| Cross-domain leak research synthesis | SUPPORTED | Phase 104 confirms the residual leak is a discontinuity-aware interpolation/visibility-classification problem. DDGI/RTXGI and Flax add per-probe distance/visibility; Unity APV uses rendering layer masks, virtual offset, dilation, density, and adjustment volumes; light-field probes add per-texel visibility; SDF/SDFGI approaches add geometry sign/distance; bilateral grids and finite-volume limiters show the same math pattern from adjacent domains: interpolation near discontinuities needs a reference/classifier or limiter. | Next prototype must add or consume source-backed boundary/geometry state and prove same-side preservation plus cross-side exclusion. Do not reopen thresholds, naive adjacent fallback, scalar/WebGL truth, or raw resolution-only sweeps. |

## Failed / Rejected Shortcuts

- Rejected: slicing by line count without contract ownership.
- Rejected: preserving narrative proof payload because it might be useful later.
- Rejected: moving harness-only helpers into thin modules.
- Rejected: replacing linear hit-confidence blending with any-hit Chebyshev ownership; focused proof smoke showed the same four weak-crush escapes and slightly worse rendered ratios.
- Rejected: raising visibility-depth angular resolution for this patch; 12px/16px trade residual leak improvement for directional-suppression regression, while 10px adds cost without moving the residual gate.

## Next Adversarial Pressure

Before merge/commit, rerun the focused no-build verifier set and review the diff for accidental generated-file or line-ending churn.
